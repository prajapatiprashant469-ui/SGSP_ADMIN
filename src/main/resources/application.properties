spring.application.name=${SPRING_APPLICATION_NAME:SGSP_CENTER}
# MongoDB Atlas URI (default). You can override with env var SPRING_DATA_MONGODB_URI
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://prajapatiprashant469:Prashant_12345@cluster0.kalmnmm.mongodb.net/SGSP_ADMIN?retryWrites=true&w=majority&appName=Cluster0}
# Mongo database name (default). You can override with env var SPRING_DATA_MONGODB_DATABASE
spring.data.mongodb.database=${SPRING_DATA_MONGODB_DATABASE:SGSP_ADMIN}
# Use env var PORT if set; otherwise pick a random available port (0) to avoid port-in-use errors during local runs.
server.port=${PORT:0}

# Prevent MongoDB auto-configuration while debugging startup issues.
# This avoids attempts to connect to Atlas during ApplicationContext refresh.
spring.autoconfigure.exclude=\
org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration,\
org.springframework.boot.autoconfigure.mongo.MongoReactiveAutoConfiguration,\
org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration,\
org.springframework.boot.autoconfigure.data.mongo.MongoReactiveDataAutoConfiguration
